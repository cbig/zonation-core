#include "io.h"

bool saveZSSICoordinatesFile(const ZSSICoordinatesFile& ssiCoordinatesFile, QIODevice& device, ErrorCallback& err)
{
  ZError::err_msg(QObject::tr("not yet implemented"), zeError);
  return false;
}
